#!/usr/bin/env python3

def commands():
    command_list = [""]
    command = input("Enter commands to run.")
    command_list.append(command)
    print(command_list)

if __name__ == "__main__":
    commands()
